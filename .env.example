# Frontend Configuration (Vite)
# Get your Client ID from: https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here
# Use /api for Vercel deployment, or http://localhost:3000/api for local development
VITE_API_BASE_URL=/api

# Backend Configuration (Vercel / Serverless)
# These variables must be set in your Vercel Project Settings
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require"
GOOGLE_CLIENT_ID=your_google_client_id_here
# GOOGLE_CLIENT_SECRET=your_google_client_secret_here (Optional: if you implement server-side code exchange)

# Local Backend Development (if running locally)
PORT=3000